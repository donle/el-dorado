# El Dorado 部署配置
# 复制为 .env（.env 在 .gitignore 里），改成你的实际值
# 服务器上只需要在首次部署时配一次

# 镜像完整名（CI 推送到 ghcr.io 后用这个）
IMAGE=ghcr.io/donle/el-dorado

# 容器名
CONTAINER=el-dorado

# 宿主机暴露给 Caddy 的端口（容器内固定 3000）
PORT=3000